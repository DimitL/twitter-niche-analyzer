{
  "name": "@twitter-niche-analyzer/backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "browsers:install": "playwright install chromium",
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@fastify/cors": "^9.0.1",
    "@twitter-niche-analyzer/shared": "*",
    "dotenv": "^16.4.7",
    "fastify": "^4.28.1",
    "playwright": "^1.52.0"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
